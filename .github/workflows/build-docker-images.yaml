name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build-fe:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: cd
      run: cd dashboard-front
    - name: pwd
      run: pwd
#     - name: Build the Docker image
#       run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
